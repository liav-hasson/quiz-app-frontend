# Local Development Environment Configuration
# This file is for local testing with mock data (npm run dev)
# It is ignored by git and won't be committed

# Enable dev mode for local testing without Google OAuth
VITE_DEV_MODE=true

# Leave Google Client ID empty to disable real OAuth in dev mode
# When VITE_DEV_MODE=true, the app will show a dev login button instead
VITE_GOOGLE_CLIENT_ID=

# API Base URL - leave empty to use same origin (proxy in vite.config.js)
VITE_API_BASE_URL=http://localhost:5000
