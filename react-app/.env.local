# =============================================================
# Frontend Environment Configuration (React + Vite)
# =============================================================
# This file is for LOCAL DEVELOPMENT ONLY (npm run dev)
# Variables prefixed with VITE_ are embedded into the frontend build at compile time
# This file is .gitignored and NOT committed to version control

# --------------------------------------------------------------
# Development Mode
# --------------------------------------------------------------
# VITE_DEV_MODE: Shows the "Dev Access Bypass" button on login screen
# - true: Shows dev login button (useful when Google OAuth isn't configured)
# - false: Hides dev login button (production mode)
# Note: This is separate from Google authentication - it's just UI
VITE_DEV_MODE=true

# --------------------------------------------------------------
# Google OAuth Configuration
# --------------------------------------------------------------
# VITE_GOOGLE_CLIENT_ID: Your Google OAuth 2.0 Client ID
# Get this from: https://console.cloud.google.com/apis/credentials
# This Client ID must have your origin (http://localhost:3000) in the authorized origins
# The backend also needs this same Client ID to verify the tokens
VITE_GOOGLE_CLIENT_ID=827337954909-n9dkebe3lr2tt2lh7t3id3msith4dsff.apps.googleusercontent.com

# --------------------------------------------------------------
# Backend API Configuration
# --------------------------------------------------------------
# VITE_API_BASE_URL: The URL where your Flask backend is running
# - http://localhost:5000: Docker backend (default for local development)
# - Empty string: Use Vite's proxy (defined in vite.config.js, routes /api to backend)
# NOTE: This does NOT affect docker-compose. Docker backend runs independently.
VITE_API_BASE_URL=http://localhost:5000

# VITE_MULTIPLAYER_URL: The URL where the WebSocket multiplayer server is running
# - http://localhost:5001: Docker multiplayer backend (default for local development)
VITE_MULTIPLAYER_URL=http://localhost:5001
